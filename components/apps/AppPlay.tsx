import Handy from "lib/thehandy";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const AppPlay = ({ handy }: { handy: Handy }): JSX.Element => {
    return (
        <div>
            <h1 className="text-2xl">Local Funscript Playing is coming soon(ish)!</h1>
        </div>
    );
};

export default AppPlay;
