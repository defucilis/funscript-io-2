import { Funscript } from "lib/funscript-utils/types";
import Modifier from "./Modifier";

export default class ModifierRandomizer extends Modifier {
    public apply(funscript: Funscript): Funscript {
        console.log("Todo!");
        return funscript;
    }
}
